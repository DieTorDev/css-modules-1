import Result from '../result/Result';
import Summary from '../summary/Summary';

const MainContainer = () => {
	return (
		<main>
			<Result />
			<Summary />
		</main>
	);
};

export default MainContainer;
